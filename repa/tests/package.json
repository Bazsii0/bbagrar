{
  "name": "bbagrar-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:rest": "jest --config jest.config.cjs rest/",
    "test:selenium": "jest --config jest.config.cjs selenium/ --testTimeout=60000",
    "test:all": "jest --config jest.config.cjs --testTimeout=60000"
  },
  "devDependencies": {
    "axios": "^1.7.0",
    "jest": "^29.7.0",
    "selenium-webdriver": "^4.20.0"
  }
}
